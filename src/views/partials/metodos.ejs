<script>

$(document).ready(function(){
   //código a ejecutar cuando el DOM está listo para recibir instrucciones.
   $(document).on("click","#busca-cc",function(event) {
        event.preventDefault();
        var cc = $('#cuentaCorriente').val()
        if(!cc){
            alert('Debe ingresar una cuenta corriente');
            return;
        }
        console.log(' dio click en buscar cc  el valor es' + cc);
        // var pathname = window.location.pathname;
        var pathname = '/getEpicrisisHospitalizadosByHospId/';
        window.location.replace(pathname + cc);

    });

    //código a ejecutar cuando el DOM está listo para recibir instrucciones.
   $(document).on("click","#busca-cm",function(event) {
        event.preventDefault();
        var cc = $('#cuentaCorriente').val()
        if(!cc){
            alert('Debe ingresar una cuenta corriente');
            return;
        }
        console.log(' dio click en buscar cc  el valor es' + cc);
        // var pathname = window.location.pathname;
        var pathname = '/creaEHdetectEntities/';
        window.location.replace(pathname + cc);

    });
});
    
    

</script>